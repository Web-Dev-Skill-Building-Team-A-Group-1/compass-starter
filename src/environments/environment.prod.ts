export const environment = {
  production: true,
  firebase: {
    apiKey: import.meta.env.NG_APP_FIREBASE_API_KEY,
    // REPLACE WITH REMAINING CONFIG
  },
  firebaseLogs: {
    apiKey: import.meta.env.NG_APP_FIREBASE_LOGS_API_KEY,
    // REPLACE WITH CONFIG
  },
};
